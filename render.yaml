services:
  # Backend Web Service
  - type: web
    name: hashout-jira-backend
    env: node
    plan: free
    rootDir: server
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: SMTP_USER
        sync: false
      - key: SMTP_PASS
        sync: false
      - key: NODE_VERSION
        value: 18

  # Frontend Static Site
  - type: web
    name: hashout-jira-portal
    env: static
    plan: free
    buildCommand: npm install && npm run build
    publishDir: dist
    envVars:
      - key: VITE_API_URL
        value: https://hashout-jira-backend.onrender.com/api
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
